obstacle_layer:
  plugin: "nav2_costmap_2d::ObstacleLayer"
  enabled: True
  observation_sources: scan
  scan:
    topic: /scan  # This matches your C++ publisher
    data_type: "LaserScan"
    marking: True
    clearing: True